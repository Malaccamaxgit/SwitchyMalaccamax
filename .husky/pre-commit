#!/bin/sh
# Husky pre-commit hook - Windows wrapper

# On Windows, call the .cmd script which handles it properly
if command -v cmd.exe > /dev/null 2>&1; then
  exec cmd.exe /c ".husky\pre-commit.cmd"
else
  # Fallback for non-Windows
  node scripts/pre-commit-security.js
fi
